default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  url: <%= ENV['DATABASE_URL'] %>

development:
  <<: *default
  database: my_api_development
  username: avongs
  password: B1983ball#
  host: localhost

test:
  <<: *default
  database: my_api_test
  username: avongs
  password: B1983ball#
  host: localhost

# Define the primary_production anchor
primary_production: &primary_production
  <<: *default
  database: my_api_production

production:
  <<: *primary_production

  cache:
    <<: *primary_production
    database: my_api_production_cache
    migrations_paths: db/cache_migrate

  queue:
    <<: *primary_production
    database: my_api_production_queue
    migrations_paths: db/queue_migrate

  cable:
    <<: *primary_production
    database: my_api_production_cable
    migrations_paths: db/cable_migrate
